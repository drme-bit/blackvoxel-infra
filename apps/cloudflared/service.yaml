apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-creds
  namespace: blackvoxel
type: Opaque
# NOTE: placeholder - do NOT commit real credentials.json in plaintext.
# Replace the following `stringData` entry locally, or use SealedSecrets / ExternalSecrets to manage this securely.
stringData:
  credentials.json: |
    credentials.json
